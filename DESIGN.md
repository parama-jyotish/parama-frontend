# PARAMA — DESIGN.md

PARAMA フロントエンド（`parama-frontend`）のデザイン憲法。Claude Code は UI 実装時に**必ず本書を参照**し、色・タイポ・レイアウトのすべての判断を本書から導出すること。本書とモック（`src/modification/after/`）が食い違う場合は、まずカズマに確認する。

- **Status**: v1.1（2026-06-24）
- **確定事項**: ブランドのビジュアル方針＝本書の方向（フルブリード配色＋夜空ヒーロー）。カラートークン・フォント役割・ロゴアセットともに確定。以前のオフホワイト基調ドラフトは**破棄**。
- **残課題**: 固定ヘッダーCTA の A/B（線色）切替挙動は実装指示書で規定。
- **配置**: 本ファイルはプロジェクトルート（`parama-frontend/parama-frontend/DESIGN.md`、git 追跡対象）に置き、`CLAUDE.md` から参照させて毎回読み込ませる。

---

## 0. ブランドスタンス（視覚に関わる範囲のみ）

> この節はプロジェクト文脈からの整理。**確定はカズマ**。

- PARAMA は「自己理解のための**分析ツール**」であり、占い・運勢サービスではない。視覚も“現代的で洗練された分析”の佇まいを保つ。
- 夜空（月・星）の写真は**ヒーローの正規表現として使用可**。ただし占い的キッチュ（水晶玉・タロット・漫画的な12星座記号・装飾的なキラキラ）は使わない。
- 断定・煽り（運命／宿命／当たる 等）を UI マイクロコピーに使わない。

---

## 1. カラートークン

すべて確定値（カズマ提供）。実装では下記 CSS 変数を単一の出典とし、ハードコードした HEX を散在させない。

### グルーピング

**背景**
| token | hex | 用途 |
|---|---|---|
| `--c-citron` | `#d6db89` | ヒーロー背景／暗背景上の枠線CTA(B) |
| `--c-cream` | `#f4f2c9` | Insights・まずはラグナを知る・診断結果(ベース)・フッタのメニュー文字 |
| `--c-cream-2` | `#f8f6ed` | 診断結果の結果テキスト枠（ベースより薄い） |
| `--c-blush` | `#ffd2d5` | Timing・Wisdom 背景／フッタの法務リンク |
| `--c-rose` | `#cd849d` | Nature 背景 |
| `--c-lilac` | `#a08ccb` | フッタ背景 |

**ブランド紫系**
| token | hex | 用途 |
|---|---|---|
| `--c-plum` | `#755d8f` | ワードマーク「Parama」（ヒーロー／フッタ） |
| `--c-plum-dark` | `#69537e` | コピーライト |
| `--c-violet` | `#583bc2` | ヒーローのタグライン |
| `--c-violet-2` | `#5841bb` | ヒーローのボタン下注釈 |
| `--c-ink` | `#311c7e` | 塗りボタンの文字／枠線CTA hover時の文字 |

**ピンク**
| token | hex | 用途 |
|---|---|---|
| `--c-pink` | `#d493a9` | 塗りボタンの面／枠線CTA hover時の面 |

**緑〜ティール系（セクション見出し・本文）**
| token | hex | 用途 |
|---|---|---|
| `--c-green` | `#79ad76` | Insights：✶・見出し・本文／まずはラグナを知る：見出し・本文 |
| `--c-green-light` | `#97bf95` | Insights：英字大見出し |
| `--c-teal-green` | `#7fa999` | Timing/Wisdom：英字大見出し・✶ |
| `--c-teal` | `#3e8a7a` | Timing/Wisdom：小見出し・本文／枠線CTA(A)の線・文字 |
| `--c-sage` | `#c6d2bf` | Nature：英字大見出し・✶ |
| `--c-sage-light` | `#d3e1cc` | Nature：小見出し・本文 |

> ✶ と Timing/Wisdom の英字見出しの色は `#7fa999` に統一済み。

### CSS 変数（そのまま使用可）

```css
:root {
  --c-citron: #d6db89;
  --c-cream: #f4f2c9;
  --c-cream-2: #f8f6ed;
  --c-blush: #ffd2d5;
  --c-rose: #cd849d;
  --c-lilac: #a08ccb;
  --c-plum: #755d8f;
  --c-plum-dark: #69537e;
  --c-violet: #583bc2;
  --c-violet-2: #5841bb;
  --c-ink: #311c7e;
  --c-pink: #d493a9;
  --c-green: #79ad76;
  --c-green-light: #97bf95;
  --c-teal-green: #7fa999;
  --c-teal: #3e8a7a;
  --c-sage: #c6d2bf;
  --c-sage-light: #d3e1cc;
}
```

Tailwind を使う場合はこの CSS 変数経由で `theme.extend.colors` に紐付ける（HEX をクラスに直書きしない）。

---

## 2. 配色マップ（どの色をどこに）

### セクション背景（フルブリード）
- **ヒーロー**：`--c-citron`
- **Insights**：`--c-cream`
- **Timing**：`--c-blush`
- **Nature**：`--c-rose`
- **Wisdom**：`--c-blush`
- **まずはラグナを知る**：`--c-cream`
- **フッタ**：`--c-lilac`
- **診断結果（ベース）**：`--c-cream`／**結果テキスト枠**：`--c-cream-2`

### テキスト配色（セクション別）

| セクション | 英字大見出し | ✶ | 和文小見出し | 本文 | その他 |
|---|---|---|---|---|---|
| ヒーロー | — | — | — | — | ワードマーク `--c-plum`／タグライン `--c-violet`／注釈 `--c-violet-2` |
| Insights | `--c-green-light` | `--c-green` | `--c-green` | `--c-green` | — |
| Timing | `--c-teal-green` | `--c-teal-green` | `--c-teal` | `--c-teal` | — |
| Nature | `--c-sage` | `--c-sage` | `--c-sage-light` | `--c-sage-light` | 小見出しは「自分の特性／がわかる」 |
| Wisdom | `--c-teal-green` | `--c-teal-green` | `--c-teal` | `--c-teal` | — |
| まずはラグナを知る | — | — | 見出し `--c-green` | `--c-green` | — |
| フッタ | — | — | — | — | メニュー `--c-cream`／ワードマーク `--c-plum`／法務リンク `--c-blush`／コピーライト `--c-plum-dark` |

> **区切り記号 ✶**：セクション見出し下の装飾記号は **✶（U+2736）＝ PARAMA のブランドマーク**。色は上表の「✶」列に従う。字形はフォント依存のため、固定したい場合は SVG 化も検討（実装時に表示確認）。

---

## 3. ボタン（コンポーネント）

### Primary（塗り）— ヒーロー／まずはラグナを知る
- 面：`--c-pink`／文字：`--c-ink`／形状：ピル（角丸大）
- hover：未指定（暫定：面をわずかに沈める）

### CTA「ラグナを知る」（枠線）— 固定ヘッダー右肩／各セクション右肩
- 面：透明
- 線・文字（**背景の明度で切替**）：**A（淡色背景上）**＝`--c-teal`（Insights/Timing/Wisdom）／**B（濃色背景上）**＝`--c-citron`（Nature）
- hover：面 `--c-pink`／文字 `--c-ink`／**線の色は変えない**

> 固定ヘッダーCTAは単一の固定要素としてスクロール追従。A/B が背景明度で切り替わるため、Nature をまたぐ瞬間に線・文字色を A↔B で切替（詳細は実装指示書）。

---

## 4. タイポグラフィ

### 欧文（確定）
- **ワードマーク「Parama」**：`logo-wordmark.svg`（アウトライン化SVG、厳密カーニング焼き込み済み・フォント非依存）。`currentColor` で `--c-plum` に着色（ヒーロー・フッタ共通）。**The Seasons はワードマーク用には不要**（他用途がなければ Adobe kit から外す）。
- **英字大見出し（Insights / Timing / Nature / Wisdom）**：Inria Serif（`inria-serif` / weight 400、Adobe kit `ijv0xyz`、fallback `serif`）。

### ブランドマーク（確定）
- 紋章ロゴ：`logo-mark.svg`（多色固定・1:1）。左肩ロゴ（ヒーロー＋Insights以降の固定ロゴ）に使用。`width`/`height` 属性は外して viewBox 基準で可変に。各セクション背景での視認性を実装時に確認。

### 和文（確定）
利用書体：`Noto Sans JP` / `ryo-text-plusn`(300/400/500) / `kinuta-maruminshinano-stdn`(400) / `kinuta-maruminfuji-stdn`(400)

| 役割 | フォント |
|---|---|
| 和文の大見出し／タグライン | `kinuta-maruminshinano-stdn` |
| 小見出し | `kinuta-maruminshinano-stdn` |
| 本文・リード | `ryo-text-plusn`（本文400 / 強調500 / キャプション300） |
| UI・注釈・フッタ・法務・フォーム | `Noto Sans JP` |

> `kinuta-maruminfuji-stdn` は現状どの役割にも未割当（kit には含まれるため読み込みは可能。任意で外してよい）。
> 大見出しと小見出しは同書体のため、**サイズ・ウェイトで階層**を付ける。サイズスケール（px）はモック実測に合わせて実装時に確定。

### フォント読み込み（実装メモ）
- **Adobe Fonts**：Inria Serif ＋ 和文3書体は同一 kit（`ijv0xyz`）。**kit ローダは1回だけ**読み込む（提供スクリプトの重複は不要）。Next.js では `<head>` で1回、または `next/script`。The Seasons は不要。
- **Noto Sans JP** は Adobe kit 外。Google Fonts または `next/font/google` で読み込む。
- `font-display: swap` を基本に、ワードマークはSVGのためレイアウトシフトの懸念なし。

---

## 5. レイアウト原則
- **モバイルファースト、基準幅 375px**（既存 `/start` LP と統一）。デスクトップ専用レイアウトは作らない。
- セクションは**フルブリード**（左右余白なしの全面背景色）。隣接セクションの背景色が直接接する。
- 縦の余白は大きめにとり「間（ま）」を活かす。
- **ヘッダ帯は背景と同色＝透明**。固定ロゴ／CTA はどのセクション背景上でも可読なコントラストを保つこと。
- スクロール演出（夜空リベール、固定挙動、オーバーレイ）は実装指示書で規定。

## 6. 品質フロア（明記せず常に満たす）
- レスポンシブにモバイルまで破綻しない。
- キーボードフォーカスを可視化（`:focus-visible`）。
- `prefers-reduced-motion` を尊重し、スクロール演出を簡略化する代替を用意。
- 文字コントラストを確保（特に淡色オン淡色／濃色オン濃色）。

## 7. 禁止・実装上の注意
- 紫・ピンクは**本デザインの正規パレット**であり禁止ではない（旧ドラフトの「スピ系配色禁止」は撤回）。
- 占い的キッチュ装飾は不可（§0）。
- 角丸・影は控えめ基調（ピル型 Primary ボタンは例外）。
- CSS セレクタの詳細度に注意：型ベース（`.section`）と要素ベース（`.cta`）のクラスが余白で打ち消し合いやすい。セクション間の margin/padding は責務を分けて設計する。

---

## 署名要素（Signature）
このサイトを記憶づける一点は、**ヒーローの「正方形ウィンドウから夜空が立ち上がるスクロール・リベール」**。装飾はここに集中させ、他は静かに保つ。

## 変更履歴
- **v1.1**（2026-06-24）ワードマークを `logo-wordmark.svg`（アウトライン）に変更し The Seasons をワードマーク用途から除外。ブランドマーク `logo-mark.svg` を確定。
- **v1.0**（2026-06-21）カラートークン全確定（区切りを ✶ に、teal-green を `#7fa999` に統一）、フォント役割確定、タグライン色 `#583bc2` 確定。
- **v0.1**（2026-06-21）初版。
