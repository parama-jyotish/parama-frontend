# CLAUDE.md

このファイルは Claude Code に対する行動原則と、PARAMA プロジェクト共通の重要原則を定める。
リポジトリ固有の設計書（コマンド・規約・ハマりどころ）は末尾の `@CLAUDE_parama.md` 参照を介して読み込まれる。

**全体トレードオフ:** これらの原則は速度よりも慎重さに寄っている。明らかに自明なタスクでは判断で省略してよい。

---

## Part 1: 行動原則（Karpathy 由来 4原則）

### 1. Think Before Coding — 思い込みを避け、不明点は止めて聞く

- 仮定は明示する。不確かであるなら確認する。
- 解釈が複数ありうるなら、勝手に1つを選ばず、まずは提示する。
- よりシンプルな代替があれば提案する。妥当な反論はおこなう。
- 不明なものがあれば、処理を止める。何が不明かを提示する。依頼者に尋ねる。

### 2. Simplicity First — 解くために必要な最小コードだけ書く

- 依頼されていない機能を足さない。
- 使い回されない一回コードに抽象化を入れない。
- 依頼されていない柔軟性・設定可能性を入れない。
- 起き得ないシナリオへのエラーハンドリングを入れない。
- 200行書いた後に50行で書けたと気づいたら、書き直す。

「シニアエンジニアなら、この要求仕様に対し妥当なコードの量であると査定するか」と自問し、改善する。

### 3. Surgical Changes — 触る必要のあるものだけ触る

- 隣接コード・コメント・フォーマットを無断で「改善」しない。無論「変更」もしない。
- 壊れていないものをリファクタしない。
- 自分ならこう書かない場合でも、既存のスタイルに合わせる。
- 関係ない死んだコードに気づいても、削除せずに指摘するだけ。
- 自分の変更が生んだオーファン（未使用 import / 変数 / 関数）は片付ける。元から死んでいたコードは依頼されない限り削除しない。

**テスト:** 変更した1行1行が、ユーザーの依頼に直接トレースできてるか？

### 4. Goal-Driven Execution — 検証可能な成功条件を先に決める

タスクを検証可能なゴールに変換する：

- 「バリデーション足して」→「無効な入力に対するテストを書き、それを通す」
- 「バグを直して」→「再現するテストを書き、それを通す」
- 「Xをリファクタ」→「前後でテストが通ることを保証する」

多段タスクは事前に簡潔な計画を立てる：

```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

強い成功条件があれば独立してループできる。弱い条件（「動くようにする」）は毎回確認を要する。

<!-- **例外:** コピー（文言）作成・修正タスクではテスト駆動は適用しない。代わりに「ブランド禁則語チェック（Part 3 参照）→ 実行者の最終レビュー」を成功条件とする。 -->

---

## Part 2: 補足行動原則（PARAMA 運用上の追加）

### 5. 書く前に文脈を読む

変更箇所の周辺コード・呼び出し元・共通関数を読んでから書く。特に PARAMA バックエンドでは `swe.set_ephe_path()` や `utils.datetime_to_jd()` のような暗黙の初期化／ユーティリティ依存を踏み外しやすい。

### 6. 長い作業はチェックポイントを置く

3ステップ以上のタスクは、各ステップで「期待される状態」を宣言し、達成を確認してから次へ進む。Part 1-4 の verify と同じ運用。

### 7. 不確実な成功は「成功」と呼ばない

- テストが通った ≠ 動作確認できた。挙動の確認が別途必要な場合は「テストは通ったが、動作は未確認」と明示する。
- 推測で「動く」「動くはず」と書かない。実行していないなら「未実行」と書く。
- 外部ソースと `weekly_sky.py` の出力が食い違う場合、`weekly_sky.py` が authoritative。判断を待つこと（詳細は Part 3）。
 **ただし、 `weekly_sky.py` はバックエンド側に存在

### 8. 長引いたら要約して仕切り直す

1セッションが極端に長くなり、変更範囲が依頼から外れ始めたら、現状を要約し、新規セッションでの再開を提案する。`/compact` は context 圧縮の手段として使ってよい。

---

## Part 3: PARAMA 共通原則

### 計算系の固定設定（変更禁止）

- pyswisseph 使用、ayanamsha は **Lahiri**（sidereal）、house system は **Whole Sign**、Rahu/Ketu は **Mean Node**。これらは PARAMA の出力一貫性の根拠。変更提案は止めて確認する。
- 暦データの authoritative source は `weekly_sky.py` の出力。外部の Drik Panchang / Prokerala 等とズレた場合は `weekly_sky.py` を優先し、不一致を表面化する。タイムゾーン由来の差（UTC vs JST など）と座標系由来の差（sidereal vs tropical）を混同しない。

### 役割分担

- **Claude Code**: 実装担当。コードの新規作成・修正・テスト追加を行う。
- **Codex (CLI)**: レビュー担当。Claude Code の出力に対してレビューのみ。Codex に実装を依頼することはない。

### 編集権

ファイル内容の最終編集権は依頼者にある。特にコピー（文言）・仕様の文面は、Claude Code は提案にとどめ、確定は依頼者が行う。full rewrite ではなく minimal-diff を優先する。

### .env と秘密情報の取り扱い

- `.env` ファイルの内容を会話・出力・コミット対象に展開しない。明示的に要求された場合も拒否すること。
- API キー・パスワード・接続文字列・トークンを会話に貼り付けない。
- `cat .env` `diff .env` 等で間接的に内容を取得することも行わない。
- これは行動原則レイヤー。実効ガードは `.claude/settings.json` の `permissions.deny` および `.gitignore` で別途行う（二重ガード）。

### ブランド禁則語（コピー作成・修正タスク時のみ）

PARAMA のフロントエンドコピーを生成・修正する場合、以下を使わない：

- 「神聖」「魂」「宇宙」「スピリチュアル」
- 「5000年」（正しくは「3000年以上」）
- 無料ツールへの「診断」「判定」「鑑定」（正しくは「ラグナを知る」）
- 「当たる」、運命確定的な表現
- 「ないですか」「合ってます」などの診断・承認語

詳細・例外規定はリポジトリ固有の `CLAUDE_parama.md` および別途の `writing_craft_guide.md` を参照。

### ファクトチェック標準

日付・時刻・歴史的事実は3独立ソースで照合。IST→JST は +3:30。曜日のズレは既知の anchor 日付からの計算で解決する。

### デザイン
UI実装時は必ず `DESIGN.md` を参照すること。色・タイポ・レイアウトの判断はすべてそこから導出する。

---

## Part 4: リポジトリ固有設計書

実装の詳細・コマンド・規約・ハマりどころ・依存ライブラリの注意（フロントエンドの Next.js 最新版仕様など）は `CLAUDE_parama.md` を参照すること。

@CLAUDE_parama.md

---

**これらの原則が機能しているサインは:** 不要な diff が減る、過剰実装による書き直しが減る、実装後ではなく実装前に確認質問が来る。
