# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# Environment variables — never commit secrets#
.env
!.env.example
!.env.*.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Secrets / credentials
secrets/
credentials/
*.key

# Claude Code local-only settings (machine-specific overrides)
.claude/settings.local.json

# User Bkup
*.bkup

# ローカル作業ファイル（2026-07-23 整理）
session_log_*
*.OLD
*BKUP
src/temp/
public/images/notuse/
修正点0629.txt
writing_craft_guide.md
PARAMA_frontend_実装指示.md
src/app/legal/src/

# エージェント向け作業ルールの入口（ローカルのみ・2026-08-18）
# Codex CLI は cwd 直下の AGENTS.md しか自動で読まない（親を遡らない）ため各リポジトリに置く。
# 実体は .claude/CLAUDE.md 側。リポジトリには含めない方針。
AGENTS.md
